
#include <stdio.h>

int main( )
{
   // C is "steam-oriented", each statement can
   // spill over many lines
   
   printf 
               (     // strings cannot be split over a line
       "Hello,
        world!\n"


      )
               ;   // every stmt ends with semi-colon

   return 0;       // every stmt ends with semi-colon
}

